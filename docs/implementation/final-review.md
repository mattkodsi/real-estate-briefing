# Final independent release review

Reviewed the combined branch and current root working-tree changes against 2da606e, including migrations001–003, Edge callers, frontend integration, deployment script, portable test setup, and prior review reports. Root supplied current production schema/RPC/credential verification and recent browser verification at 390/440/desktop; those observations were not independently repeated. No production calls, mutations, deployments, or test reruns were performed during this final review.

No new confirmed production rollout blocker was found. The earlier independently reproduced rates regression and cross-tab queue loss are fixed in the combined branch. Production `days.date` and `weeks.week_of` are date columns, so migration002's baseline comparison is compatible; registry keys and queue fields match the reviewed SQL. `reader_profile_api(jsonb,text)` matches the Edge invocation in `supabase/functions/reader-profile/index.ts` and replacement migration003.

Required release sequence remains concrete: apply migration001 before Python/Edge worker changes; apply migration002 immediately before the new dispatcher/capture functions so the previous-day baseline is current; apply migration003 before the reader-profile handler and frontend delta client. The old reader function can otherwise acknowledge an unknown mutations property without applying it. The root app callback forwards mutations and observes the loaded baseline (`js/app.js`, profile store initialization/activateProfile); ticket capture passes a transient ticket; calendar notification destinations are handled. Root confirmed the private owner credential is configured. Do not probe push-dispatch for verification because it sends queued notifications.

`package.json`, the lockfile and `.github/workflows/test.yml` now make PGlite tests portable through the pinned npm dependency and Node24 CI; missing PGlite fails rather than silently skipping. Root will install dependencies on the main checkout before deploy. `scripts/deploy.sh` requires main, refuses preexisting staged changes, runs tests/checks, bumps version strings, stages frontend asset paths and verifies hosted hashes afterward. Its asset globs would include any other dirty JS/CSS, so the current reviewed working tree must be committed before release and main kept clean; the inspected current asset set contains only this release's changes. New root files still untracked during this review must be included in that implementation commit before merging (package/lock, helper modules, verify_release.py and CI/test files).

One confirmed completeness limitation was sent to root, not treated as an existing-runtime regression: `scripts/publication.py:publish_document` stamps document publishedAt but no per-story summaryPublishedAt; `supabase/functions/fill-content/index.ts:210` updates article content without the per-story enrichedAt/enrichedBy that the Python merge adds. Therefore current code does not establish complete per-article summary/enrichment timing across every worker. The corrective contract is a generation instruction, not evidence those timestamps exist. Final delivery should either fix this narrow gap or describe it explicitly. No newsletter-arrival or real-device delivery timing was established.

Other retained limits are already documented in backend-review.md/publication-review.md: legacy clients keep full-array replacement semantics until upgraded; market refreshes are not a distributed lock; cross-reason alert dedupe is profile-scoped but not one transaction across simultaneously competing dispatcher runs. None is newly introduced by this release. Hosted Deno behavior and resulting live asset bytes remain release-stage verification rather than completed local-test claims.
